<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Toko Amer - Premium Marketplace</title>
    
    <link rel="icon" href="data:,">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- FontAwesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Tailwind Configuration -->
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['"Plus Jakarta Sans"', 'sans-serif'],
                    },
                    colors: {
                        card: '#2a2d35',
                        abu_transparan: 'rgba(44, 47, 51, 0.7)',
                    }
                }
            }
        }
    </script>

    <!-- Custom CSS -->
    <style>
        /* EFEK BACKGROUND NEON ABU */
        body {
            /* Memancarkan cahaya neon abu-abu dari atas ke bawah */
            background: radial-gradient(circle at 50% 0%, #6c6f7d 0%, #2c2f33 50%, #1a1c1e 100%);
            background-attachment: fixed;
            color: #ffffff;
            -webkit-tap-highlight-color: transparent;
        }
        
        .text-gold {
            background: linear-gradient(to right, #D4AF37, #FFDF73, #D4AF37);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .btn-gold {
            background: linear-gradient(to right, #bf953f, #fcf6ba, #b38728, #fbf5b7, #aa771c);
            background-size: 200% auto;
            color: #000;
            transition: 0.5s;
        }

        .btn-gold:hover {
            background-position: right center;
        }

        /* Pendaran (Glow) Neon Abu pada Kartu Produk */
        .neon-abu-border {
            border: 1px solid rgba(150, 150, 150, 0.3);
            transition: all 0.4s ease;
        }
        .neon-abu-border:hover {
            border-color: rgba(200, 200, 200, 0.9);
            box-shadow: 0 0 25px rgba(200, 200, 200, 0.3);
        }

        .modal-overlay {
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s ease-out, visibility 0.3s ease-out;
        }
        
        .modal-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .modal-content {
            transform: scale(0.95) translateY(20px);
            opacity: 0;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .modal-overlay.active .modal-content {
            transform: scale(1) translateY(0);
            opacity: 1;
        }

        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #1a1c1e; }
        ::-webkit-scrollbar-thumb { background: #6c6f7d; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #D4AF37; }
    </style>
</head>
<body class="antialiased flex flex-col min-h-screen relative">

    <!-- Navbar -->
    <nav class="fixed w-full z-40 bg-abu_transparan backdrop-blur-md border-b border-gray-500/30 transition-all duration-300 shadow-[0_4px_30px_rgba(0,0,0,0.1)]">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-20">
                <!-- Logo -->
                <div class="flex-shrink-0 flex items-center cursor-pointer">
                    <span class="text-2xl md:text-3xl font-extrabold tracking-tighter text-gold drop-shadow-md">TOKO AMER</span>
                </div>
                
                <!-- Navigasi Kanan -->
                <div class="flex items-center space-x-4 md:space-x-8">
                    <a href="#koleksi" class="hidden md:block text-gray-300 hover:text-white transition-colors text-sm font-semibold tracking-wider uppercase drop-shadow-sm">Koleksi</a>

                    <!-- Navigasi Otentikasi (TOMBOL LOGIN/SIGN UP) -->
                    <div id="navAuthArea">
                        <button onclick="openModal('login')" class="btn-gold px-6 py-2.5 rounded-full text-xs md:text-sm font-bold tracking-wide uppercase shadow-[0_0_15px_rgba(212,175,55,0.4)]">LOGIN / SIGN UP</button>
                    </div>
                    
                    <!-- Logout Area (Muncul jika sudah login) -->
                    <div id="navLogoutArea" class="hidden flex items-center bg-black/40 px-4 py-2 rounded-full border border-gray-500/50 shadow-[0_0_10px_rgba(255,255,255,0.1)]">
                        <span id="navUserName" class="text-xs md:text-sm font-bold text-gold mr-3"></span>
                        <button onclick="logoutUser()" class="text-gray-400 hover:text-red-500 transition-colors text-lg" title="Logout"><i class="fa-solid fa-right-from-bracket"></i></button>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <header class="relative pt-32 pb-20 lg:pt-48 lg:pb-32 overflow-hidden flex-grow-0 z-10">
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h1 class="text-5xl md:text-7xl font-extrabold tracking-tight mb-6 text-white drop-shadow-lg">
                Eksklusivitas dalam <br/>
                <span class="text-gold drop-shadow-[0_0_15px_rgba(212,175,55,0.5)]">Setiap Serat.</span>
            </h1>
            <p class="mt-4 text-lg md:text-xl text-gray-300 max-w-2xl mx-auto mb-10 font-light drop-shadow-md">
                Temukan koleksi apparel premium dengan desain mahakarya. Dibuat untuk mereka yang memahami nilai estetika tingkat tinggi.
            </p>
        </div>
    </header>

    <!-- Product Grid Section -->
    <main id="koleksi" class="flex-grow bg-transparent py-10 relative z-10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between mb-12">
                <h2 class="text-3xl font-bold tracking-tight text-white drop-shadow-md">Koleksi <span class="text-gold">Terbaru</span></h2>
                <div class="h-[1px] bg-gray-500/40 flex-grow ml-8 shadow-[0_0_10px_rgba(255,255,255,0.2)]"></div>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
                <!-- Produk 1 -->
                <div class="group bg-card/80 backdrop-blur-sm rounded-2xl overflow-hidden neon-abu-border">
                    <div class="relative aspect-[4/5] overflow-hidden bg-black/50">
                        <img src="produk1.png" onerror="this.onerror=null; this.src='https://placehold.co/600x750/23272A/D4AF37?text=Giyuu+Tomioka'" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700 ease-in-out">
                    </div>
                    <div class="p-6 relative z-10">
                        <h3 class="text-lg font-bold mb-1 text-white drop-shadow-md">Kaos Giyuu Exclusive</h3>
                        <p class="text-gray-300 text-sm mb-4">Cotton Combed 24s Premium</p>
                        <span class="text-xl font-extrabold text-white block mb-4 drop-shadow-md">Rp 185.000</span>
                        <button onclick="beliProduk('Kaos Giyuu Exclusive', '185000')" class="w-full border border-gray-400 hover:border-transparent py-3 rounded-xl text-sm font-bold uppercase hover:btn-gold transition-all duration-300 bg-white/5">
                            Beli Via WA
                        </button>
                    </div>
                </div>

                <!-- Produk 2 -->
                <div class="group bg-card/80 backdrop-blur-sm rounded-2xl overflow-hidden neon-abu-border">
                    <div class="relative aspect-[4/5] overflow-hidden bg-black/50">
                        <img src="produk2.png" onerror="this.onerror=null; this.src='https://placehold.co/600x750/23272A/D4AF37?text=Berserk+Oversized'" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700 ease-in-out">
                    </div>
                    <div class="p-6 relative z-10">
                        <h3 class="text-lg font-bold mb-1 text-white drop-shadow-md">Berserk Oversized Tee</h3>
                        <p class="text-gray-300 text-sm mb-4">Heavyweight Cotton 16s</p>
                        <span class="text-xl font-extrabold text-white block mb-4 drop-shadow-md">Rp 210.000</span>
                        <button onclick="beliProduk('Berserk Oversized Tee', '210000')" class="w-full border border-gray-400 hover:border-transparent py-3 rounded-xl text-sm font-bold uppercase hover:btn-gold transition-all duration-300 bg-white/5">
                            Beli Via WA
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-black/30 border-t border-gray-600/50 py-8 mt-auto z-10 relative backdrop-blur-sm">
        <div class="max-w-7xl mx-auto px-4 text-center">
            <span class="text-2xl font-bold tracking-tighter text-gold opacity-50 block mb-4 drop-shadow-md">TOKO AMER</span>
            <p class="text-gray-400 text-sm">© 2026 Toko Amer. All rights reserved.</p>
        </div>
    </footer>

    <!-- Floating WhatsApp Button (Selalu Aktif) -->
    <button onclick="hubungiAdmin()" class="fixed bottom-6 right-6 z-40 bg-green-500 hover:bg-green-600 text-white w-14 h-14 rounded-full flex items-center justify-center text-3xl shadow-[0_0_25px_rgba(34,197,94,0.6)] hover:scale-110 transition-transform duration-300">
        <i class="fa-brands fa-whatsapp"></i>
    </button>

    <!-- MODAL DASHBOARD ADMIN RAHASIA -->
    <div id="adminModal" class="modal-overlay fixed inset-0 z-50 flex items-center justify-center bg-black/95 backdrop-blur-md px-4">
        <div class="modal-content bg-card border border-red-500/50 w-full max-w-4xl rounded-3xl overflow-hidden shadow-[0_0_30px_rgba(255,0,0,0.2)] relative p-8">
            <button onclick="closeAdmin()" class="absolute top-4 right-4 w-8 h-8 rounded-full bg-white/5 hover:bg-white/10 text-white z-10"><i class="fa-solid fa-xmark"></i></button>
            <h3 class="text-2xl font-bold text-red-500 mb-6"><i class="fa-solid fa-user-secret"></i> Dashboard Toko Amer (Rahasia)</h3>
            
            <div class="flex border-b border-white/10 mb-6">
                <button onclick="switchAdminTab('orders')" id="adminTabOrders" class="flex-1 py-3 text-center font-bold text-sm uppercase text-gold border-b-2 border-yellow-500 transition-colors">Data Pesanan</button>
                <button onclick="switchAdminTab('users')" id="adminTabUsers" class="flex-1 py-3 text-center font-bold text-sm uppercase text-gray-500 border-b-2 border-transparent transition-colors">Data Pengguna</button>
            </div>

            <div id="adminViewOrders" class="block overflow-x-auto max-h-[60vh]">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="border-b border-white/10 text-gold text-xs uppercase tracking-wider bg-black/40 sticky top-0">
                            <th class="py-3 px-4">Waktu</th>
                            <th class="py-3 px-4">Pembeli</th>
                            <th class="py-3 px-4">Produk</th>
                            <th class="py-3 px-4 text-center">Qty</th>
                            <th class="py-3 px-4 text-right">Total (Rp)</th>
                        </tr>
                    </thead>
                    <tbody id="adminTableOrders" class="text-sm text-gray-300"></tbody>
                </table>
            </div>

            <div id="adminViewUsers" class="hidden overflow-x-auto max-h-[60vh]">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="border-b border-white/10 text-gold text-xs uppercase tracking-wider bg-black/40 sticky top-0">
                            <th class="py-3 px-4">Nama Lengkap</th>
                            <th class="py-3 px-4">Email</th>
                            <th class="py-3 px-4">Password (Kelihatan)</th>
                        </tr>
                    </thead>
                    <tbody id="adminTableUsers" class="text-sm text-gray-300"></tbody>
                </table>
            </div>

            <div class="mt-6 flex justify-end space-x-4 border-t border-white/10 pt-4">
                <button onclick="hapusDatabase('orders')" class="bg-orange-600/20 hover:bg-orange-600 text-orange-400 hover:text-white border border-orange-600 px-4 py-2 rounded-lg text-xs font-bold transition-colors">Kosongkan Pesanan</button>
                <button onclick="hapusDatabase('users')" class="bg-red-600/20 hover:bg-red-600 text-red-400 hover:text-white border border-red-600 px-4 py-2 rounded-lg text-xs font-bold transition-colors">Kosongkan Akun User</button>
            </div>
        </div>
    </div>

    <!-- Auth Pop-Up Modal -->
    <div id="authModal" class="modal-overlay fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm px-4">
        <div class="modal-content bg-card border border-gray-500/50 w-full max-w-md rounded-3xl overflow-hidden shadow-[0_0_30px_rgba(200,200,200,0.15)] relative">
            <button onclick="closeModal()" class="absolute top-4 right-4 w-8 h-8 rounded-full bg-white/5 hover:bg-white/10 text-gray-400 hover:text-white transition-colors z-10"><i class="fa-solid fa-xmark"></i></button>

            <div class="flex border-b border-gray-600/50 bg-black/40">
                <button id="tabLogin" onclick="switchAuthTab('login')" class="flex-1 py-5 text-center font-bold text-sm tracking-widest uppercase text-gold border-b-2 border-yellow-500">Login</button>
                <button id="tabSignup" onclick="switchAuthTab('signup')" class="flex-1 py-5 text-center font-bold text-sm tracking-widest uppercase text-gray-400 hover:text-gray-200 border-b-2 border-transparent">Sign Up</button>
            </div>

            <div class="p-8 bg-card/90">
                <!-- FORM LOGIN -->
                <form id="formLogin" class="block">
                    <div class="text-center mb-6">
                        <h3 class="text-2xl font-bold text-white mb-1 drop-shadow-md">Masuk Akun</h3>
                    </div>
                    <div class="space-y-4">
                        <input type="email" id="loginEmail" class="w-full bg-black/40 border border-gray-500 rounded-xl py-3 px-4 text-white focus:border-gray-300 focus:outline-none focus:ring-1 focus:ring-gray-300 transition-all shadow-inner" placeholder="Email Anda" required>
                        <input type="password" id="loginPassword" class="w-full bg-black/40 border border-gray-500 rounded-xl py-3 px-4 text-white focus:border-gray-300 focus:outline-none focus:ring-1 focus:ring-gray-300 transition-all shadow-inner" placeholder="Password" required>
                        <button type="button" onclick="prosesLogin()" class="w-full btn-gold py-3.5 rounded-xl text-sm font-bold uppercase mt-4 shadow-[0_0_15px_rgba(212,175,55,0.3)]">Masuk</button>
                    </div>
                </form>

                <!-- FORM SIGN UP -->
                <form id="formSignup" class="hidden">
                    <div class="text-center mb-6">
                        <h3 class="text-2xl font-bold text-white mb-1 drop-shadow-md">Buat Akun</h3>
                    </div>
                    <div class="space-y-4">
                        <input type="text" id="signupNama" class="w-full bg-black/40 border border-gray-500 rounded-xl py-3 px-4 text-white focus:border-gray-300 focus:outline-none focus:ring-1 focus:ring-gray-300 transition-all shadow-inner" placeholder="Nama Lengkap" required>
                        <input type="email" id="signupEmail" class="w-full bg-black/40 border border-gray-500 rounded-xl py-3 px-4 text-white focus:border-gray-300 focus:outline-none focus:ring-1 focus:ring-gray-300 transition-all shadow-inner" placeholder="Email Anda" required>
                        <input type="password" id="signupPassword" class="w-full bg-black/40 border border-gray-500 rounded-xl py-3 px-4 text-white focus:border-gray-300 focus:outline-none focus:ring-1 focus:ring-gray-300 transition-all shadow-inner" placeholder="Buat Password" required>
                        <button type="button" onclick="prosesDaftar()" class="w-full btn-gold py-3.5 rounded-xl text-sm font-bold uppercase mt-4 shadow-[0_0_15px_rgba(212,175,55,0.3)]">Daftar Sekarang</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- SCRIPT UTAMA -->
    <script>
        const ADMIN_EMAIL = "admin@tokoamer.com";
        const ADMIN_PASS = "AmerAdmin2026!#";
        const waNumberIntl = "6285260286998";
        let activeUser = JSON.parse(localStorage.getItem('tokoAmerSession')) || null;

        window.onload = function() {
            updateNavbarUI();
        };

        function formatRupiah(angka) {
            return new Intl.NumberFormat('id-ID').format(angka);
        }

        function beliProduk(namaProduk, hargaSatuan) {
            if (!activeUser) {
                alert("Silakan Login terlebih dahulu untuk memesan.");
                openModal('login');
                return;
            }
            if (activeUser.role === 'admin') {
                alert("Akun Administrator tidak digunakan untuk membeli produk.");
                return;
            }

            let inputQty = prompt(`Pesanan: ${namaProduk}\nHarga: Rp ${formatRupiah(hargaSatuan)}\n\nBerapa jumlah (pcs) yang ingin dibeli?`, "1");
            let qty = parseInt(inputQty);
            
            if (qty && qty > 0) {
                let totalHarga = parseInt(hargaSatuan) * qty;
                let dbOrders = JSON.parse(localStorage.getItem('tokoAmerOrders')) || [];
                dbOrders.push({
                    waktu: new Date().toLocaleString('id-ID'),
                    pembeli: activeUser.nama,
                    email: activeUser.email,
                    produk: namaProduk,
                    qty: qty,
                    total: totalHarga
                });
                localStorage.setItem('tokoAmerOrders', JSON.stringify(dbOrders));

                const pesanWA = `Halo Admin Toko Amer,\nSaya ${activeUser.nama} ingin memesan:\n\nProduk: ${namaProduk}\nJumlah: ${qty} pcs\nTotal Harga: Rp ${formatRupiah(totalHarga)}\n\nBagaimana langkah pembayaran selanjutnya?`;
                window.open(`https://wa.me/${waNumberIntl}?text=${encodeURIComponent(pesanWA)}`, '_blank');
            } else if(inputQty !== null) {
                alert("Jumlah tidak valid. Batal memesan.");
            }
        }

        function hubungiAdmin() {
            let nama = activeUser ? activeUser.nama : "Pengunjung";
            const pesan = `Halo Admin Toko Amer, saya ${nama} ingin bertanya tentang produk Anda.`;
            window.open(`https://wa.me/${waNumberIntl}?text=${encodeURIComponent(pesan)}`, '_blank');
        }

        function prosesDaftar() {
            const nama = document.getElementById('signupNama').value;
            const email = document.getElementById('signupEmail').value.trim();
            const password = document.getElementById('signupPassword').value;

            if(!nama || !email || !password) {
                alert("Isi semua data!"); return;
            }
            if(email === ADMIN_EMAIL) {
                alert("Email tidak bisa digunakan."); return;
            }

            let dbUsers = JSON.parse(localStorage.getItem('tokoAmerUsers')) || [];
            if(dbUsers.find(user => user.email === email)) {
                alert("Email sudah terdaftar!"); return;
            }

            dbUsers.push({ nama: nama, email: email, password: password, role: 'user' });
            localStorage.setItem('tokoAmerUsers', JSON.stringify(dbUsers));

            alert("Pendaftaran Sukses! Silakan masuk (Login).");
            document.getElementById('signupNama').value = '';
            document.getElementById('signupEmail').value = '';
            document.getElementById('signupPassword').value = '';
            switchAuthTab('login');
        }

        function prosesLogin() {
            const email = document.getElementById('loginEmail').value.trim();
            const password = document.getElementById('loginPassword').value;

            if(!email || !password) {
                alert("Masukkan email dan password!"); return;
            }

            if (email === ADMIN_EMAIL && password === ADMIN_PASS) {
                activeUser = { nama: "ADMINISTRATOR", email: email, role: 'admin' };
                localStorage.setItem('tokoAmerSession', JSON.stringify(activeUser));
                closeModal();
                updateNavbarUI();
                alert("Akses Rahasia Diterima! Membuka Dashboard Admin...");
                openAdmin(); 
                document.getElementById('loginEmail').value = '';
                document.getElementById('loginPassword').value = '';
                return;
            }

            let dbUsers = JSON.parse(localStorage.getItem('tokoAmerUsers')) || [];
            let userValid = dbUsers.find(user => user.email === email && user.password === password);

            if(userValid) {
                activeUser = { nama: userValid.nama, email: userValid.email, role: 'user' };
                localStorage.setItem('tokoAmerSession', JSON.stringify(activeUser));
                alert("Login Sukses! Hai, " + userValid.nama);
                closeModal();
                updateNavbarUI();
                document.getElementById('loginEmail').value = '';
                document.getElementById('loginPassword').value = '';
            } else {
                alert("Email atau password salah.");
            }
        }

        function logoutUser() {
            activeUser = null;
            localStorage.removeItem('tokoAmerSession');
            updateNavbarUI();
            alert("Anda telah logout.");
        }

        function updateNavbarUI() {
            const navAuth = document.getElementById('navAuthArea');
            const navLogout = document.getElementById('navLogoutArea');
            const navName = document.getElementById('navUserName');

            if (activeUser) {
                navAuth.classList.add('hidden');
                navLogout.classList.remove('hidden');
                if (activeUser.role === 'admin') {
                    navName.innerHTML = `<button onclick="openAdmin()" class="hover:text-white transition-colors bg-red-600/30 border border-red-500/50 px-3 py-1 rounded-md text-red-400 shadow-[0_0_15px_rgba(255,0,0,0.3)]"><i class="fa-solid fa-user-secret mr-1"></i> ADMIN DASHBOARD</button>`;
                } else {
                    navName.innerHTML = activeUser.nama;
                }
            } else {
                navAuth.classList.remove('hidden');
                navLogout.classList.add('hidden');
            }
        }

        function openAdmin() {
            if(!activeUser || activeUser.role !== 'admin') return;
            refreshAdminTables();
            document.getElementById('adminModal').classList.add('active');
            switchAdminTab('orders'); 
        }

        function closeAdmin() {
            document.getElementById('adminModal').classList.remove('active');
        }

        function switchAdminTab(tab) {
            const vUsers = document.getElementById('adminViewUsers');
            const vOrders = document.getElementById('adminViewOrders');
            const tUsers = document.getElementById('adminTabUsers');
            const tOrders = document.getElementById('adminTabOrders');
            
            const activeStyle = "flex-1 py-3 text-center font-bold text-sm uppercase text-gold border-b-2 border-yellow-500 transition-colors";
            const inactiveStyle = "flex-1 py-3 text-center font-bold text-sm uppercase text-gray-500 border-b-2 border-transparent transition-colors";

            if(tab === 'users') {
                vUsers.classList.remove('hidden'); vUsers.classList.add('block');
                vOrders.classList.remove('block'); vOrders.classList.add('hidden');
                tUsers.className = activeStyle; tOrders.className = inactiveStyle;
            } else {
                vOrders.classList.remove('hidden'); vOrders.classList.add('block');
                vUsers.classList.remove('block'); vUsers.classList.add('hidden');
                tOrders.className = activeStyle; tUsers.className = inactiveStyle;
            }
        }

        function refreshAdminTables() {
            const tbUsers = document.getElementById('adminTableUsers');
            const dbUsers = JSON.parse(localStorage.getItem('tokoAmerUsers')) || [];
            tbUsers.innerHTML = '';
            
            if(dbUsers.length === 0) {
                tbUsers.innerHTML = '<tr><td colspan="3" class="py-4 text-center text-gray-400 italic">Belum ada akun yang terdaftar.</td></tr>';
            } else {
                dbUsers.forEach(user => {
                    tbUsers.innerHTML += `
                        <tr class="border-b border-gray-600/30 hover:bg-white/5 transition-colors">
                            <td class="py-3 px-4 font-semibold text-white">${user.nama}</td>
                            <td class="py-3 px-4 text-gray-400">${user.email}</td>
                            <td class="py-3 px-4 text-red-400 font-mono">${user.password}</td>
                        </tr>`;
                });
            }

            const tbOrders = document.getElementById('adminTableOrders');
            const dbOrders = JSON.parse(localStorage.getItem('tokoAmerOrders')) || [];
            tbOrders.innerHTML = '';

            if(dbOrders.length === 0) {
                tbOrders.innerHTML = '<tr><td colspan="5" class="py-4 text-center text-gray-400 italic">Belum ada riwayat pesanan.</td></tr>';
            } else {
                dbOrders.reverse().forEach(order => {
                    tbOrders.innerHTML += `
                        <tr class="border-b border-gray-600/30 hover:bg-white/5 transition-colors">
                            <td class="py-3 px-4 text-xs text-gray-400">${order.waktu}</td>
                            <td class="py-3 px-4 font-semibold text-white">${order.pembeli}<br><span class="text-xs text-gray-500 font-normal">${order.email}</span></td>
                            <td class="py-3 px-4 text-gold">${order.produk}</td>
                            <td class="py-3 px-4 text-center font-bold text-white">${order.qty}</td>
                            <td class="py-3 px-4 text-right font-bold text-green-400">${formatRupiah(order.total)}</td>
                        </tr>`;
                });
            }
        }

        function hapusDatabase(target) {
            if(target === 'orders') {
                if(confirm("Yakin hapus seluruh riwayat pesanan pembeli?")) {
                    localStorage.removeItem('tokoAmerOrders');
                    refreshAdminTables();
                }
            } else if (target === 'users') {
                if(confirm("Yakin hapus seluruh akun pelanggan? Mereka harus daftar ulang.")) {
                    localStorage.removeItem('tokoAmerUsers');
                    refreshAdminTables();
                }
            }
        }

        const authModal = document.getElementById('authModal');
        function openModal(tab = 'login') {
            authModal.classList.add('active');
            switchAuthTab(tab);
        }
        function closeModal() {
            authModal.classList.remove('active');
        }
        function switchAuthTab(tab) {
            document.getElementById('formLogin').classList.add('hidden');
            document.getElementById('formSignup').classList.add('hidden');
            
            const active = "flex-1 py-5 text-center font-bold text-sm tracking-widest uppercase text-gold border-b-2 border-yellow-500";
            const inactive = "flex-1 py-5 text-center font-bold text-sm tracking-widest uppercase text-gray-400 hover:text-gray-200 border-b-2 border-transparent transition-colors";

            if (tab === 'login') {
                document.getElementById('formLogin').classList.remove('hidden');
                document.getElementById('tabLogin').className = active;
                document.getElementById('tabSignup').className = inactive;
            } else {
                document.getElementById('formSignup').classList.remove('hidden');
                document.getElementById('tabSignup').className = active;
                document.getElementById('tabLogin').className = inactive;
            }
        }
    </script>
</body>
</html>
